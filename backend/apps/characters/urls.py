from django.urls import path, include
from rest_framework.routers import DefaultRouter

# Por enquanto, URLs vazias (vamos implementar depois)
urlpatterns = [
    # TODO: Adicionar URLs quando criarmos as views
]